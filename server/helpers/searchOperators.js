const operator = {
    AND: "and",
    OR: "or"
}

const orderParams = {
    ASC: "asc",
    DESC: "desc"
};

const bookParams = {
    NAME: "name",
    YEAR: "year",
    AUTHOR: "author"
};

const userParams = {
    NAME: "name",
    LASTNAME: "lastname",
    ADDRESS: "address",
    NUMBER: "number"
};

module.exports = {
    operator,
    orderParams,
    bookParams,
    userParams
};